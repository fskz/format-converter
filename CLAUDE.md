# 项目说明

## 格式转换器 (Format Converter)

Excel (.xlsx) ↔ JSON 格式互转工具。

## 开发偏好

- 设计文档不提交到 git，仅保留在本地 `docs/superpowers/specs/` 目录

## Git 提交规范

所有提交备注必须使用**中文**编写，遵循以下格式：

```
<类型>: <简短中文描述>

<可选的详细说明>
```

### 提交类型

| 类型 | 用途 |
|------|------|
| `feat` | 新功能 |
| `fix` | 修复缺陷 |
| `perf` | 性能优化或代码改进 |
| `refactor` | 代码重构，不改变功能 |
| `docs` | 文档更新 |
| `test` | 测试相关 |
| `build` | 构建或部署相关 |
| `style` | 代码格式调整 |

### 示例

```
fix: 修复日志xml标签解析错误

perf: 统一模型追踪管道

feat: 添加数据导出功能

refactor: 重构t状态管理逻辑
```